배열 : int i = 1; 처럼 선언 하듯이 선언을 하지만 int [] iArr = {10,20,30} ; 로 3개의 방을 가지고 내부에 순서대로 해당 값을 가진다.
	배열을 선언을 하게되면 메모리에 배열을 생성 하고 그 데이터값이 할당을 하게 되지만 배열의 크기, 즉 방의 숫자는 한번 선언이 되어지면
	변경이 불가능하다. (내부 데이터값을 바꿀수있다.)
	다차원 배열도 선언을 할수있는데 서로 다른 아파트 (주소값) 과 서로 다른 호수 (데이터값)를 가져서 각자 다른 배열이다.